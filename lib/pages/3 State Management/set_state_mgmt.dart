import 'package:flutter/material.dart';

class SetStateMgmt extends StatelessWidget {
  const SetStateMgmt({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}