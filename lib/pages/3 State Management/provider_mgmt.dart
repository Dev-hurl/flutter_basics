import 'package:flutter/material.dart';

class ProviderMgmt extends StatelessWidget {
  const ProviderMgmt({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}